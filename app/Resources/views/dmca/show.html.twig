{% extends 'base.html.twig' %}

{% block body %}
    <h1>DMCA</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ dMCA.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ dMCA.title }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ dMCA.content }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('dmca_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('dmca_edit', { 'id': dMCA.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
