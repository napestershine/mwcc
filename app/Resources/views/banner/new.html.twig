{% extends 'adminbase.html.twig' %}

{% block pageheader %}New Banner{% endblock %}
{% block container %}

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('banner_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
